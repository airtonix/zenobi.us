each article in articles
  h3: a(href=site.url+article.url+"#disqus_thread", data-disqus_identifier=article.url.slugify())= article.title
    small.block.date= moment.utc(article.date).format('DD. MMMM YYYY')
