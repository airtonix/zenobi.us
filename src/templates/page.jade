extends layout

block append vars
	- bodyclass = 'page'

block prepend title
	| #{ page.title + " - "}

block content
	#page-header
		h1
			block content-title
				= page.metadata.title

	.row
		.large-6.large-centered.columns
			block content-html
				article.article
					section.content!= typogr(page.html).typogrify()

