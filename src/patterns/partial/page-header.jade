.large-8.columns.hero.vertical(class=page.metadata?page.metadata.headerClass:'')
    h1(class=pageIcon(page))
        +banner(page)
        !=page.metadata.title
        small.block=page.metadata.subtitle
