import { ArgsTable, Meta, Story, Canvas  } from '@storybook/addon-docs/blocks';
import { Site } from './Site'
import { Page } from './Page'

export const Template = (args) => <Site {...args } />;

<Meta title="Pages/Site" component={Site} />

# Site

The main layout.

has a top area and a footer area

<Canvas>
  <Story name="basic" args={{
      contentSlot: (<div style={{backgroundColor: 'skyblue', minHeight: '320px'}} />),
      footerSlot: (<div style={{backgroundColor: 'pink', minHeight: '128px'}} />)
  }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={Site} />
