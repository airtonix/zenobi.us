import { ArgsTable, Meta, Story, Canvas  } from '@storybook/addon-docs/blocks';
import { Site } from './Site'
import { Page } from './Page'
import { BrandingBlock, BrandingBlockStyles } from './BrandingBlock'


export const Template = (args) => <Site {...args } />;

<Meta title="Pages/Site" component={Site} />

# Site

The main layout.

has a top area and a footer area

<Canvas>
  <Story name="basic" args={{
      contentSlot: (<BrandingBlock style={BrandingBlockStyles.Hero} />)
  }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={Site} />

# With Page Layout


<Canvas>
  <Story name="page layout" args={{
      contentSlot: (
        <Page
            asideSlot={( <BrandingBlock style={BrandingBlockStyles.Badge} /> )}
        >
            Some content
        </Page>
      )
  }}>
    {Template.bind({})}
  </Story>
</Canvas>
