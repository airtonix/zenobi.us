import { ArgsTable, Meta, Story, Canvas  } from '@storybook/addon-docs/blocks';
import { Page } from './Page'
import { Block } from './Block'
import { LoremIpsum, Avatar, name, surname, username } from 'react-lorem-ipsum'

export const Template = (args) => (<Page {...args} /> )

<Meta title="Pages/Page" component={Page} />

# Page

Has a sidebar and a content section.

in Mobile, the sidebar collapses offscreen, and is opened by a hamburger button.

<Canvas>
  <Story name="basic" args={{
      asideSlot: (<Block>
        <Avatar gender="male" className="avatar" width="200" height="200" alt="Avatar" />
        <div className="name">{name('male')}</div>
        <div className="surname">{surname()}</div>
        <div className="username">{username()}</div>
      </Block>),
      children: (<LoremIpsum p={8} />),
  }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={Page} />
