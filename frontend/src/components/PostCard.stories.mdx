import { ArgsTable, Meta, Story, Canvas  } from '@storybook/addon-docs/blocks';
import { PostCard } from './PostCard'

export const Template = (args) => <PostCard {...args } />;

<Meta title="Components/PostCard" component={PostCard} />

# PostCard

A special block of small information

<Canvas>
  <Story name="basic">
    <PostCard
      title='A Title'
      summary='Some hefty content that might have descenders present.'
    />
  </Story>
</Canvas>

<ArgsTable of={PostCard} />

## With Tags

<Canvas>
  <Story name="tags">
    <PostCard
      title='A Title'
      summary='Some hefty content that might have descenders present.'
      tags={['A', 'Collection', 'of', 'Tags']}
    />
  </Story>
</Canvas>
