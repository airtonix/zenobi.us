import { ArgsTable, Meta, Story, Canvas  } from '@storybook/addon-docs/blocks';
import { Hamburger } from './Hamburger'

export const Template = (args) => <Hamburger {...args} />

<Meta title="Components/Hamburger" component={Hamburger} />

# Hamburger

A Control that allows to opening and closing a menu

<Canvas>
    <Story name="basic" args={{ size: 32, color: 'red' }}>
      {Template.bind({ onClick: () => {} })}
    </Story>
</Canvas>

<ArgsTable of={Hamburger} />
