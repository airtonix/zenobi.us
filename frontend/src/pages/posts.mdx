import Sugar from 'sugar'
import { FileListWithRouteData } from '~/components/FileListWithRouteData'
import { PostCard } from '~/components/PostCard'

# Thoughts

<FileListWithRouteData
  query={`files^(>date).{
    'title': title,
    'path': path,
    'date': date
  }`}
  component={PostCard}
/>
